name: APEX Continuous Integration

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  backend-test:
    name: Backend Test Suite & ML Verification
    runs-on: ubuntu-latest
    services:
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: apex_db
          POSTGRES_USER: apex_user
          POSTGRES_PASSWORD: apex_password
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      REDIS_URL: redis://localhost:6379/0

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: "latest"

      - name: Install Dependencies
        run: uv sync

      - name: Run Pytest Suite
        run: uv run pytest -v

      - name: Verify Benchmark Evaluation
        run: uv run python benchmarks/run_benchmarks.py

      - name: Run Automated Evaluation & Regression Scoring Harness
        run: uv run python backend/eval/run_eval.py

  frontend-build:
    name: Frontend TypeScript & Vite Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install Frontend Dependencies
        working-directory: frontend
        run: npm ci

      - name: Build Production Frontend
        working-directory: frontend
        run: npm run build

  docker-compose-test:
    name: Docker Compose Stack Integration Test
    runs-on: ubuntu-latest
    needs: [backend-test, frontend-build]
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and Start Stack
        env:
          DOCKER_BUILDKIT: 1
          COMPOSE_DOCKER_CLI_BUILD: 1
        run: docker compose up -d --build

      - name: Wait for APEX Services Health
        run: |
          HEALTHY=0
          for i in {1..35}; do
            if curl -s -f http://127.0.0.1:8000/api/health > /dev/null 2>&1; then
              echo "APEX Application is UP and HEALTHY on iteration $i!"
              HEALTHY=1
              break
            fi
            echo "Waiting for APEX backend to be healthy ($i/35)..."
            sleep 3
          done
          if [ $HEALTHY -ne 1 ]; then
            echo "Healthcheck timed out! Showing container logs:"
            docker compose logs
            exit 1
          fi

      - name: Verify Health Endpoint Response
        run: |
          RESPONSE=$(curl -s http://127.0.0.1:8000/api/health)
          echo "Health response: $RESPONSE"
          echo "$RESPONSE" | grep -q '"status":"ok"'

      - name: Stop and Teardown Stack
        if: always()
        run: docker compose down -v
