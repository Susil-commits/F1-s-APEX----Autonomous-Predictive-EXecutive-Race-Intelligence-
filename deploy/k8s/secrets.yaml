apiVersion: v1
kind: Secret
metadata:
  name: apex-secrets
  namespace: default
  labels:
    app: apex
type: Opaque
stringData:
  DATABASE_URL: "postgresql+asyncpg://apex_user:apex_password@apex-postgres:5432/apex_db"
  REDIS_URL: "redis://apex-redis:6379/0"
  APEX_JWT_SECRET: "apex-super-secret-key-enterprise-k8s-production-f1"
