# APEX — F1 Pre-Race Predictor (V1) — Environment Configuration
# Copy to .env and set your own values.

# API host/port (Uvicorn)
PORT=8000
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8000,http://127.0.0.1:5173
